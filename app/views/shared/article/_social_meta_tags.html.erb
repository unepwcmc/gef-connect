<%= content_for (:fb_title) { label } %>
<%= content_for (:fb_description) { description } %>
<%= content_for (:fb_image) { image } %>
<%= content_for (:fb_url) { url } %>
<%= content_for (:fb_site_name) { site_name } %>
<%= content_for (:tw_title) { label } %>
<%= content_for (:tw_description) { description } %>
<%= content_for (:tw_image) { image } %>
