<%= content_for (:title) { label } %>
<%= content_for (:description) { description } %>
<%= content_for (:image) { image } %>
<%= content_for (:url) { url } %>
<%= content_for (:site_name) { site_name } %>
